# Water Quality webapp

Vite + React + shadcn/ui SPA. Design tokens, shell layout, and Geist typography match the Work Force Management ESMS app.

```bash
cp .env.example .env
npm install
npm run dev
```
